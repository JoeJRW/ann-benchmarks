float:
  any:
  - base_args: ['@metric']
    constructor: AliSQL
    disabled: false
    docker_tag: ann-benchmarks-alisql
    module: ann_benchmarks.algorithms.alisql
    name: alisql
    run_groups:
      normal:
        arg_groups: [{txt: 'test', M: [4, 5, 6, 8, 12, 16, 24, 32, 48], engine: 'InnoDB'}]
        args: {}
        query_args: [[10, 20, 30, 40, 80, 120, 200, 400, 800]]

